# =============================================================================
# Cyberlearnix Helm Values — K3s single-node (no Azure, SSH-only)
#
# Architecture:
#   • K3s on the existing Azure VM (IP: 20.197.21.226)
#   • Docker Compose handles: Postgres, Redis, Prometheus, Grafana, Loki, Promtail
#   • K3s handles:           10 Spring Boot microservices
#   • Spring Boot pods reach Postgres/Redis via VM's private IP (injected at deploy)
#
# Usage (from setup script / CI):
#   helm upgrade --install cyberlearnix ./helm \
#     -f ./helm/values-k3s.yaml \
#     --set global.registry=$DOCKERHUB_USERNAME \
#     --set appConfig.DB_HOST=$HOST_IP \
#     --set appConfig.REDIS_HOST=$HOST_IP \
#     --set images.gateway-service=$SHA \
#     ... (one --set per service, SHA injected by CI) \
#     --namespace cyberlearnix --create-namespace
# =============================================================================

global:
  registry: "cyberlearnix"  # local docker images: cyberlearnix/cyberlearnix-<service>
  imagePrefix: "cyberlearnix-"  # Docker Hub images are named: user/cyberlearnix-<service>
  pullPolicy: IfNotPresent  # images are pre-loaded into K3s containerd
  environment: production

# imagePullSecrets not needed — images imported directly into K3s containerd
imagePullSecrets: []

# Single-node: ScheduleAnyway so replicas don't stay Pending on one node
topology:
  whenUnsatisfiable: ScheduleAnyway

# ── Shared application config ─────────────────────────────────────────────────
# DB_HOST and REDIS_HOST point to Docker Compose containers on the same VM.
# They are injected via --set appConfig.DB_HOST=$VM_PRIVATE_IP at deploy time.
appConfig:
  SPRING_PROFILES_ACTIVE: "prod"
  DB_HOST: ""               # injected: --set appConfig.DB_HOST=<vm-private-ip>
  DB_PORT: "5432"
  DB_USER: "postgres"
  REDIS_HOST: ""            # injected: --set appConfig.REDIS_HOST=<vm-private-ip>
  REDIS_PORT: "6379"
  # K8s DNS — services resolve within the cyberlearnix namespace
  USER_SERVICE_URL: "http://user-service:8081"
  COURSE_SERVICE_URL: "http://course-service:8082"
  ENROLLMENT_SERVICE_URL: "http://enrollment-service:8083"
  NOTIFICATION_SERVICE_URL: "http://notification-service:8084"
  SHOP_SERVICE_URL: "http://shop-service:8085"
  FORM_SERVICE_URL: "http://form-service:8087"
  CMS_SERVICE_URL: "http://cms-service:8089"
  ADMIN_SERVICE_URL: "http://admin-service:8090"
  INSTRUCTOR_SERVICE_URL: "http://instructor-service:8091"
  CORS_ALLOWED_ORIGINS: "https://app.cyberlearnix.com,https://cyberlearnix.com"
  # Reduce connection pool: 10 services × 3 = 30 connections (well within Postgres default 100)
  SPRING_DATASOURCE_HIKARI_MAXIMUM_POOL_SIZE: "3"

# ── Ingress — K3s ships with Traefik v2 ──────────────────────────────────────
ingress:
  className: traefik
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: web
  tls:
    enabled: false          # add cert-manager later when domain is ready
    secretName: ""

# ── External access host ──────────────────────────────────────────────────────
hosts:
  api: ""                   # empty = catch-all; replace with domain name when DNS is configured

# ── Per-service overrides — single replica, HPA/PDB disabled (single node) ──
services:

  gateway-service:
    port: 8080
    containerPort: 8080
    replicas: 1
    dbRequired: false
    ingress:
      enabled: true
      path: /
      pathType: Prefix
    resources:
      requests:
        memory: "256Mi"
        cpu: "100m"
      limits:
        memory: "512Mi"
        cpu: "500m"
    hpa:
      enabled: false
    pdb:
      minAvailable: 0

  user-service:
    port: 8081
    containerPort: 8081
    replicas: 1
    dbName: cyberlearnix_users
    dbRequired: true
    ingress:
      enabled: false
    resources:
      requests:
        memory: "256Mi"
        cpu: "100m"
      limits:
        memory: "512Mi"
        cpu: "500m"
    hpa:
      enabled: false
    pdb:
      minAvailable: 0

  course-service:
    port: 8082
    containerPort: 8082
    replicas: 1
    dbName: cyberlearnix_courses
    dbRequired: true
    ingress:
      enabled: false
    resources:
      requests:
        memory: "256Mi"
        cpu: "100m"
      limits:
        memory: "512Mi"
        cpu: "500m"
    hpa:
      enabled: false
    pdb:
      minAvailable: 0

  enrollment-service:
    port: 8083
    containerPort: 8083
    replicas: 1
    dbName: cyberlearnix_enrollments
    dbRequired: true
    ingress:
      enabled: false
    resources:
      requests:
        memory: "256Mi"
        cpu: "100m"
      limits:
        memory: "512Mi"
        cpu: "500m"
    hpa:
      enabled: false
    pdb:
      minAvailable: 0

  notification-service:
    port: 8084
    containerPort: 8084
    replicas: 1
    dbRequired: false
    ingress:
      enabled: false
    resources:
      requests:
        memory: "192Mi"
        cpu: "50m"
      limits:
        memory: "384Mi"
        cpu: "250m"
    hpa:
      enabled: false
    pdb:
      minAvailable: 0

  shop-service:
    port: 8085
    containerPort: 8085
    replicas: 1
    dbName: cyberlearnix_shop
    dbRequired: true
    ingress:
      enabled: false
    resources:
      requests:
        memory: "256Mi"
        cpu: "100m"
      limits:
        memory: "512Mi"
        cpu: "500m"
    hpa:
      enabled: false
    pdb:
      minAvailable: 0

  form-service:
    port: 8087
    containerPort: 8087
    replicas: 1
    dbName: cyberlearnix_forms
    dbRequired: true
    ingress:
      enabled: false
    resources:
      requests:
        memory: "256Mi"
        cpu: "100m"
      limits:
        memory: "512Mi"
        cpu: "500m"
    hpa:
      enabled: false
    pdb:
      minAvailable: 0

  admin-service:
    port: 8090
    containerPort: 8090
    replicas: 1
    dbName: cyberlearnix_admin
    dbRequired: true
    ingress:
      enabled: false
    resources:
      requests:
        memory: "256Mi"
        cpu: "100m"
      limits:
        memory: "512Mi"
        cpu: "500m"
    hpa:
      enabled: false
    pdb:
      minAvailable: 0

  cms-service:
    port: 8089
    containerPort: 8089
    replicas: 1
    dbName: cyberlearnix_cms
    dbRequired: true
    ingress:
      enabled: false
    resources:
      requests:
        memory: "256Mi"
        cpu: "100m"
      limits:
        memory: "512Mi"
        cpu: "500m"
    hpa:
      enabled: false
    pdb:
      minAvailable: 0

  instructor-service:
    port: 8091
    containerPort: 8091
    replicas: 1
    dbName: cyberlearnix_instructor
    dbRequired: true
    ingress:
      enabled: false
    resources:
      requests:
        memory: "256Mi"
        cpu: "100m"
      limits:
        memory: "512Mi"
        cpu: "500m"
    hpa:
      enabled: false
    pdb:
      minAvailable: 0
